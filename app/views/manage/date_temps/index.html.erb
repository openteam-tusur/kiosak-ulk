<div class='col-xs-5'>
  <h3>Календарь</h3>
</div>
<div class='col-xs-7 btn-actions'>
  <%= link_to 'Добавить данные', new_manage_date_temp_path, class: "btn btn-success" %>
  <%= link_to 'Управление аудиториями', manage_places_path, class: "btn btn-success" %>
</div>

<div class='col-xs-12 list_dates'>
  <ul>
  <% @date_temps.each do |date| %>
    <li><%= link_to I18n.l(date.date_title, format: :long), manage_date_temp_path(date) %></li>
  <% end %>
  </ul>
</div>

<%= paginate @date_temps %>
